<h1 class = "index_head">Your Own Albums</h1>
<hr style="color:white;">
<h4 class="index_sub">Punjabi Singers...<h4>
<div class="row container-fluid">
  <% @albums.each do |album| %>
  <div class="col-xxl-2 col-lg-4  col-md-6 col-sm-12">
    <div class="card index_card h-100">
    <%if album.image.attached?%>
       <%= image_tag url_for(album.image) , style: "height:100%; width:100%;" %>
       <%end%>
      <div class="card-img-overlay">
        <h5 class="card-title"><%= link_to album.title, album, class:"card_title"%></h5>
      </div>
    </div>
  </div>
<% end %>
</div>